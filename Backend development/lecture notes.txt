frontend + backend = full stack devlopment.

backend works on rquest/responce.
client sends request and server sends responce

syncrnus java script code (single threded code)
asyncrunus java script code (multi threded code) (Node js)




Lecture 2

setTimeout(callback function,timeInMS)


asyncrunus line will always run after all syncrunus lines are done with